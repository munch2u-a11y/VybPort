const form=document.querySelector('#registerForm'),message=document.querySelector('#message');let login=false,invited=false;
/* The invite box only appears when the instance actually asks for one. */
fetch('/api/instance').then(r=>r.json()).then(d=>{invited=d.invite_required;document.querySelector('#inviteRow').hidden=!invited}).catch(()=>{});
async function request(path,payload){const response=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await response.json();if(!response.ok)throw new Error(data.error||'Request failed.');return data}
document.querySelector('#switchLogin').addEventListener('click',()=>{login=!login;const displayName=form.querySelector('[name="display_name"]');displayName.closest('label').hidden=login;displayName.required=!login;form.querySelector('[name="bio"]').closest('label').hidden=login;document.querySelector('#inviteRow').hidden=login||!invited;form.querySelector('button[type="submit"]').textContent=login?'Log in to VybPort →':'Create local account →';document.querySelector('#switchLogin').textContent=login?'Create an account':'Log in';message.textContent='';});
form.addEventListener('submit',async event=>{event.preventDefault();message.className='form-message';message.textContent='';const values=Object.fromEntries(new FormData(form));try{const result=await request(login?'/api/auth/login':'/api/auth/register',values);message.classList.add('ok');message.textContent=`Welcome, ${result.user.display_name}. Opening your garage…`;setTimeout(()=>location.href='./index.html',500)}catch(error){message.textContent=error.message}});
